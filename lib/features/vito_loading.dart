import 'package:flutter/material.dart';

class VitoLoading extends StatelessWidget {
  const VitoLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
